#!/bin/sh
# MCP stdio server that exposes sermon-admin admin actions as tools to Claude Code.
# Runs the CLI's `mcp` subcommand via the package-local tsx (no build step).
DIR=$(cd "$(dirname "$0")/.." && pwd)
exec "$DIR/node_modules/.bin/tsx" "$DIR/src/index.ts" mcp "$@"
